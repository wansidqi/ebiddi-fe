import React, { createContext, useContext } from "react";

const AppContext = createContext<{{ pascalCase context }}Data | null>(null);

type {{ pascalCase context }}Data = {};

export function {{ pascalCase context }}Provider(props: React.PropsWithChildren<{}>) {
  const contextValue: {{ pascalCase context }}Data = {};

  return <AppContext.Provider value={contextValue} {...props} />;
}

export function use{{ pascalCase context }}() {
  const context = useContext(AppContext);
  if (!context) {
    throw new Error("state must be used within a {{ toCamelCase context }}");
  }
  return context;
};